programa

    declare a,b,c,d.

    escreva("Programa Teste").

    escreva ("Digite A").
    leia (a:NUM).
    a := 2.
    escreva ("Digite B").
    leia (b:NUM).
    b := 2.

    se (a<b)
    entao
        c := a + b.

    senao
        c := a - b.
    fimse

    escreva ("C e igual a ").
    escreva (c).

    d := a + b.
    escreva ("D e igual a ").
    escreva (d).
fimprog.